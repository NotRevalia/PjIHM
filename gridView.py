from PyQt5.QtWidgets import QWidget
from PyQt5.QtGui import QWindow

class gridView(QWindow):
    def __init__(self):
        super().__init__()
        self.setWindowTitle("Sokoban")
        self.window
        self.window = QWidget()
        self.show()
        

